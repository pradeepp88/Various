export default class StaticTableData {
  public code: number;
  public description: string;
  constructor(code: number, description: string) {
    this.code = code;
    this.description = description;
  }
}
